const { json } = require('express');
const fs = require ('fs');
const path = require('path');

const controladorEditar = (req,res)=>{
    res.render('editar')}


    module.exports = controladorEditar