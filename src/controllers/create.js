const { json } = require('express');
const fs = require ('fs');
const path = require('path');

const controladorCreate = (req,res)=>{
    res.render('create')}


    module.exports = controladorCreate